global:
  scrape_interval: 15s

scrape_configs:
  - job_name: proxy-a
    static_configs:
      - targets: ["proxy-a:${PROXY_METRICS_INTERNAL_PORT}"]

  - job_name: proxy-b
    static_configs:
      - targets: ["proxy-b:${PROXY_METRICS_INTERNAL_PORT}"]
